<main>
    <div class="content">
        <div class="content__products">
            {{#each products}}
            <div class="shoes">
                <a href="" class="shoes__link"></a>
                <div>
                    <div class="shoes__name">
                        <h3>{{title}}</h3>
                    </div>
                    <div class="shoes__price">
                        <h4>${{price}}</h4>
                    </div>

                </div>

            </div>
            {{/each}}
        </div>
    </div>
</main>